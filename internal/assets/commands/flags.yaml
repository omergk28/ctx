# Flag descriptions for ctx CLI.
# Used by assets.FlagDesc() to populate cobra flag usage strings.
# Keys use dot notation: scope.flag-name (e.g., add.file)
#
# UPDATE THIS FILE when flags change (added, removed, renamed,
# or semantics changed). Flag descriptions appear in --help
# output and must stay accurate.
#
# See also: commands.yaml (Short/Long), examples.yaml (Example fields).

add.application:
  short: 'Application for learnings: how to apply this going forward (required for learnings)'
add.branch:
  short: Git branch name for task provenance
add.commit:
  short: Git commit hash for task provenance
add.consequence:
  short: 'Consequence for decisions: what changes as a result (required for decisions)'
add.context:
  short: 'Context for decisions: what prompted this decision (required for decisions)'
add.file:
  short: Read content from file instead of argument
add.lesson:
  short: 'Lesson for learnings: the key insight (required for learnings)'
add.priority:
  short: Priority level for tasks (high, medium, low)
add.rationale:
  short: 'Rationale for decisions: why this choice over alternatives (required for decisions)'
add.section:
  short: Target section within file
add.session-id:
  short: AI session ID for task provenance
add.share:
  short: Also publish to the ctx Hub
agent.budget:
  short: Token budget for context packet
agent.cooldown:
  short: Suppress repeated output within this duration (0 to disable)
agent.format:
  short: 'Output format: md or json'
agent.session:
  short: Session identifier for cooldown isolation (e.g., $PPID)
agent.skill:
  short: Include named skill content in context packet
agent.include-hub:
  short: Include ctx Hub entries in context packet
changes.since:
  short: 'Time reference: duration (24h) or date (2026-03-01)'
compact.archive:
  short: Create .context/archive/ for old content
initialize.caller:
  short: Identify the calling tool (e.g. vscode) to tailor output

doctor.json:
  short: Machine-readable JSON output
fmt.width:
  short: Target line width
fmt.check:
  short: Check only, exit 1 if files would change
drift.fix:
  short: Auto-fix supported issues (staleness, missing files)
drift.json:
  short: Output as JSON
guide.commands:
  short: List all CLI commands
guide.skills:
  short: List all available skills
trigger.test.path:
  short: File path for mock input
trigger.test.tool:
  short: Tool name for mock input
setup.write:
  short: Write the configuration file instead of printing
initialize.reset:
  short: Reset an existing context (interactive only; backs up existing files to .context/.backup-init-<ISO>/ before overwriting)
initialize.merge:
  short: Auto-merge ctx content into existing CLAUDE.md
initialize.minimal:
  short: Only create essential files (TASKS.md, DECISIONS.md, CONSTITUTION.md)
initialize.no-plugin-enable:
  short: Skip auto-enabling the ctx plugin in global Claude Code settings
initialize.no-steering-init:
  short: Skip scaffolding foundation steering files in .context/steering/
journal.obsidian.output:
  short: Output directory for vault
journal.site.build:
  short: Run zensical build after generating
journal.site.output:
  short: Output directory for site
journal.site.serve:
  short: Run zensical serve after generating
journal.source.all-projects:
  short: Include sessions from all projects
journal.source.full:
  short: Show full message content (with --show or --latest)
journal.source.latest:
  short: Show the most recent session
journal.source.limit:
  short: Maximum sessions to display
journal.source.project:
  short: Filter by project name
journal.source.show:
  short: Show details of a specific session by slug or ID
journal.source.since:
  short: Show sessions on or after this date (YYYY-MM-DD)
journal.source.tool:
  short: Filter by tool (e.g., claude-code)
journal.source.until:
  short: Show sessions on or before this date (YYYY-MM-DD)
journal.schema.check.dir:
  short: Directory to scan for JSONL files
journal.schema.check.all-projects:
  short: Scan all Claude Code project directories
journal.schema.check.quiet:
  short: Exit code only (0 = clean, 1 = drift)
load.budget:
  short: Token budget for assembly
load.raw:
  short: Output raw file contents without assembly
loop.completion:
  short: Completion signal to detect
loop.max-iterations:
  short: Maximum iterations (0 = unlimited)
loop.output:
  short: Output script filename
loop.prompt:
  short: Prompt file to use
loop.tool:
  short: 'AI tool: claude, aider, or generic'
memory.import.dry-run:
  short: Show classification plan without writing
memory.publish.budget:
  short: Line budget for published content
memory.publish.dry-run:
  short: Show what would be published without writing
memory.sync.dry-run:
  short: Show what would happen without writing
notify.event:
  short: Event name (required)
notify.hook:
  short: Hook name for structured detail (optional)
notify.session-id:
  short: Session ID (optional)
notify.variant:
  short: Template variant for structured detail (optional)
pad.add.file:
  short: ingest a file as a blob entry
pad.edit.append:
  short: append text to the end of the entry
pad.edit.file:
  short: replace blob file content
pad.edit.label:
  short: replace blob label
pad.edit.prepend:
  short: prepend text to the beginning of the entry
pad.edit.tag:
  short: add a tag to the entry
pad.export.dry-run:
  short: print what would be exported without writing
pad.export.force:
  short: overwrite existing files instead of timestamping
pad.root.blob:
  short: import first-level files from a directory as blob entries
pad.merge.dry-run:
  short: print what would be merged without writing
pad.merge.key:
  short: path to key file for decrypting input files
pad.show.out:
  short: write blob content to a file
pad.tag:
  short: filter entries by tag (prefix with ~ to exclude)
pad.tag.json:
  short: output tags as JSON
pause.session-id:
  short: Session ID (overrides stdin)
journal.import.all:
  short: Import all sessions from current project
journal.import.all-projects:
  short: Include sessions from all projects
journal.import.dry-run:
  short: Show what would be imported without writing files
journal.import.keep-frontmatter:
  short: Preserve enriched YAML frontmatter during regeneration
journal.import.regenerate:
  short: Re-import existing files (preserves YAML frontmatter by default)
journal.import.yes:
  short: Skip confirmation prompt
journal.lock.all:
  short: Lock all journal entries
journal.unlock.all:
  short: Unlock all journal entries
remind.add.after:
  short: Don't surface until this date (YYYY-MM-DD)
remind.after:
  short: Don't surface until this date (YYYY-MM-DD)
remind.dismiss.all:
  short: Dismiss all reminders
audit.dismiss.all:
  short: Dismiss every audit report in .context/audit/
resume.session-id:
  short: Session ID (overrides stdin)
site.feed.base-url:
  short: Base URL for entry links
site.feed.out:
  short: Output path for the generated feed
status.json:
  short: Output as JSON
status.verbose:
  short: Include file content previews
steering.sync.all:
  short: Sync to all supported tool formats
sync.dry-run:
  short: Show what would change without modifying
bootstrap.json:
  short: Output in JSON format
bootstrap.quiet:
  short: Output only the context directory path
event.all:
  short: Include rotated log file
event.event:
  short: Filter by event type
event.hook:
  short: Filter by hook name
event.json:
  short: Output raw JSONL
event.last:
  short: Show last N events
event.session:
  short: Filter by session ID
message.json:
  short: Output in JSON format
prune.days:
  short: Prune files older than this many days
prune.dry-run:
  short: Show what would be pruned without deleting
sysinfo.json:
  short: Output in JSON format
usage.follow:
  short: Stream new entries as they arrive
usage.json:
  short: Output raw JSONL
usage.last:
  short: Show last N entries
usage.session:
  short: Filter by session ID (prefix match)
system.markjournal.check:
  short: Check if stage is set (exit 1 if not)
system.pause.session-id:
  short: Session ID (overrides stdin)
system.resume.session-id:
  short: Session ID (overrides stdin)
system.sessionevent.caller:
  short: Calling editor (e.g., vscode)
system.sessionevent.type:
  short: 'Event type: start or end'
trace.collect.record:
  short: Record context refs for a post-commit hash
trace.file.last:
  short: Maximum commits to show
trace.json:
  short: Output as JSON
trace.last:
  short: Show context for last N commits
trace.tag.note:
  short: Context note to attach to the commit
task.archive.dry-run:
  short: Preview changes without modifying files
tool:
  short: 'Override active AI tool (e.g., claude, cursor, cline, kiro, codex)'
connection.token:
  short: Admin credential from hub startup
hub.start.daemon:
  short: Run the hub server in the background
hub.start.data-dir:
  short: Hub data directory (default ~/.ctx/hub-data/)
hub.start.peers:
  short: Comma-separated peer addresses for cluster mode
hub.start.port:
  short: Hub listen port (default 9900)
hub.stop.data-dir:
  short: Hub data directory (default ~/.ctx/hub-data/)
watch.dry-run:
  short: Show updates without applying
watch.log:
  short: 'Log file to watch (default: stdin)'
handover.summary:
  short: 'What happened this session (past tense). Required.'
handover.next:
  short: 'What the next agent should do FIRST. Required.'
handover.highlights:
  short: 'Notable artifacts produced this session.'
handover.open-questions:
  short: 'Things that remain undecided.'
handover.commit:
  short: 'Override resolved git HEAD for Provenance line (CI replay).'
handover.no-fold:
  short: 'Skip closeout consumption (mid-session checkpoint).'
